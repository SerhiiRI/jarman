(ns jarman.core
 ;; (:gen-class)
 ;; (:require  [jarman.gui.gui-login :as log])
  )

;; (defn -main
;;   "I don't do a whole lot ... yet."
;;   [& args]
;;   (println "Hello, World!")
;;    (log/start))
